# 隨手場記・場次人員管理 PWA

單機使用、免登入、資料全部存在你瀏覽器本機（IndexedDB）的週日場次管理工具。

## v55 更新內容
- 修正「場次的依付款方式加總，誤把已請假的季打人員金額也算進去」的問題。原因是季打人員如果曾經以單場繳費方式付過款（記錄了繳費方式），之後才被改成請假，這筆繳費方式紀錄沒有被清掉，導致統計時被重複算進去。現在改成兩處：計算邏輯本身會排除請假的季打（不管有沒有殘留的繳費方式紀錄），另外把出席狀態改成「請假」的當下，也會自動清掉這筆已經記錄的繳費方式，避免類似的殘留資料再度發生

## v54 更新內容
- 修正季打退費詳情訊息裡「只有冷氣區塊、沒有請假區塊」時會多出一條分隔線的問題。原因是頂部（總退費金額下方）的分隔線原本是固定顯示的，冷氣區塊自己前面又會加一條，兩條線正好連在一起變成重複；現在改成頂部分隔線只在「後面確實有內容」時才顯示，冷氣區塊自己的分隔線也只在「前面確實有請假區塊」時才加，各種組合（只有請假、只有冷氣、兩者都有、兩者都沒有）都驗證過分隔線數量正確

## v53 更新內容
- **真正找到並修正了退費詳情訊息推送失敗的原因**：`buildRefundDetailFlexMessage` 裡有幾個地方用了 `alignItems: 'baseline'`，但 LINE Flex Message 規格其實只接受 `flex-start`／`flex-end`／`center` 這三種值，不支援 `baseline`。這個問題只有在真的送出訊息時才會被 LINE 的 API 拒絕（回傳 400 錯誤），本機測試沒辦法透過畫面確認格式對不對，所以一直沒被抓到。已經全部改成 `center`，並新增一項會遞迴檢查整個訊息結構的測試，往後任何地方誤用了 LINE 不支援的屬性值都會在測試階段就被抓出來，不用等到真的發送失敗才發現

## v52 更新內容
- 儲存退費詳情的檢查邏輯加強：原本只看 HTTP 狀態碼是不是 200，現在會**進一步確認 Worker 回傳的「實際寫入筆數」跟送出的筆數一致**——如果 Worker 靜默跳過了某些資料（例如格式不符），原本會誤判成功，現在會正確顯示「儲存失敗」
- 瀏覽器主控台（DevTools Console）新增除錯訊息，會印出準備儲存的 memberId 清單，以及 Worker 實際回傳的內容，方便排查問題卡在哪一步

## v51 更新內容
- 修正「發送季打結算總表後，點擊 postback 查不到退費詳情」的問題。真正原因是儲存退費詳情這個步驟被設計成「不等待的背景動作」——送出成功的提示一跳出來，使用者很容易立刻把 App 切到背景或關掉，這時候手機瀏覽器常常會直接終止還在背景執行中的網路請求，導致資料根本沒送到 Worker。現在改成**等儲存也完成後，才會關閉視窗、顯示「已發送」**，確保兩個步驟一定會一起完成

## v50 更新內容
- 發送「季打結算總表」到群組成功後，現在會**自動**把每個人完整的退費詳情訊息存到 Worker（供之後點擊 postback 查詢用），不用另外操作。如果儲存失敗，群組訊息本身不會受影響（已經送出去了），只會另外跳出提示告訴你退費詳情可能查不到
- 這一步驟改用你已經在用的 LINE 通關密語（X-Api-Key）驗證，**不需要另外設定或啟用雲端同步**——如果你的群組訊息本來就能正常發送，這個新功能不用多做任何設定就會一起生效
- 需要搭配同步更新的 Worker（`sunday-roster-line-relay-v3-refund-postback.zip`，這次也一併修正了它裡面的驗證方式）

## v49 更新內容
- 季打結算總表 LINE 訊息裡，點擊姓名觸發的 postback 資料，從原本用「姓名」改成用「成員 ID」（人員資料本來就已經有 id 這個欄位，每個人建立時都會自動產生）。這是為了之後 postback 查詢退費詳情時，不會因為兩個人剛好同名就查到錯的人

## v48 更新內容
- 季打退費詳情 LINE 訊息排版調整：「請假」「冷氣」標題文字放大加深；請假次數改成小字附註在標題後面；兩個區塊的總金額都改成跟標題同一行、放在最後面，拿掉原本獨立的小計/總計行；場次日期不再顯示星期
- 訊息內容的程式碼（`buildRefundDetailFlexMessage`）已完成並測試，但**發送方式還沒接上**——下一步要處理的是 postback 的 Worker 端邏輯（接收 LINE 的 postback 事件、查出對應成員的退費資料、組訊息、用 Reply API 回覆），這部分還沒開始寫，需要先確認幾個技術前提才能動工

## v47 更新內容
- 季打退費詳情 LINE 訊息的「冷氣」區塊，標籤下方新增「冷氣總退費金額」一行，顯示無/部分使用退費加總後扣掉需補繳金額的淨額；如果淨額是「需要多收錢」而不是退費，會用「+」號跟紅色文字標示，跟底下各子類別的顏色邏輯一致

## v46 更新內容
- 冷氣退費計算再細分：原本的「退費」拆成「無（完全未使用）」跟「部分使用」兩類；新增「其他，需補繳」——當某場實際冷氣費換算下來超過季度設定的基準值時，會列出需要向該成員多收的金額（用不同顏色的區塊跟退費方向做視覺區隔）
- 新增 `buildRefundDetailFlexMessage()`：組出單一季打成員的退費詳情 LINE Flex Message，分成「請假」「冷氣」兩大部分，冷氣底下再依無/部分使用/其他三類列出對應場次與金額；沒有內容的區塊/子類別會整段省略，不會顯示空的「0場」
- **這次還沒有在畫面上加發送按鈕**——目前完成的是訊息組裝的程式碼本身（已用實際資料測試過產生的 JSON 正確），還沒有接到 UI 上的「發送」入口，等你確認訊息內容沒問題，下一步再幫你加上發送按鈕跟聊天室選擇

## v45 更新內容
- 季度設定（新增季度、季度內頁的「季度設定」）新增「預設人均冷氣費」欄位，新建季度預設帶入 45
- 季打結算邏輯新增冷氣退費：某場次實際冷氣費（換算每人、無條件進位到5的倍數）如果低於這個基準，差額會自動計入該成員的退費；請假場次的退費本身已包含冷氣費，不會重複退
- 舊季度（尚未設定過這個欄位的）不會被追溯套用冷氣退費，計算結果維持原樣，只有實際存過這個欄位的季度才會生效
- 這只是這次改動的第一步（結算計算本身），LINE 退費詳情訊息的實際程式碼還沒開始寫，等你確認示意圖沒問題後就會接著做

## v44 更新內容
1. 批量操作選單改為緊貼底部導覽列上方，中間不再留空白（用共用的高度變數對齊，不同裝置的安全區域也會正確對齊）
2. 新增人員的重複姓名檢查加上性別條件：同名但性別不同會視為不同人，正常新增
3. 場次內頁人員名單（臨打、候補）改為依加入時間排序，先加入的排前面；批量新增時則依輸入的順序排列（不會因為時間戳記太接近而排序錯亂）

## v43 更新內容
1. 季打名單「總計」卡片標題直接顯示所有付款方式加總的金額
2. 人員名單新增「常用」星號按鈕，常用人員排序在最前面；加入季打／臨打／候補的多選清單，改為只顯示常用人員
3. 新增人員（不管是人員名單本身、場次內頁加人員、季打名單加人員）如果姓名跟既有人員完全相同，會直接沿用既有人員，不會重複建立
4. 總覽的季度速覽新增「當前盈餘」，計算本季已經過期（日期已過）的場次盈餘加總
5. 場次列表的場次副標題，從「已收/應收」改為「已收/盈餘」
6. 季打名單的批量設定繳費，移除獨立的已繳/未繳選項，改成只選繳費方式，狀態自動判斷（有選方式即已繳，未指定即未繳）
7. 場次內頁人員名單批量調整選單，按鈕順序改為「繳費方式」在前、「費用調整」在後
8. 場次內頁人員名單的批量選取，現在季打人員只要不是「已預繳」狀態，也會有 checkbox 可以一起批量調整（原本只有臨打才有）
9. 批量操作列改成固定顯示在畫面底部（貼齊底部導覽列上方），不會再因為跳出這個區塊讓整個頁面往下移動；場次內頁的人員名單也補上「全選」功能（季打名單原本就有）

## v42 更新內容
- 季打名單tab的「總計」卡片改為只計算實際有預繳的金額，沒有預繳、只是單場繳費的季打不會計入這裡（改用獨立的計算，跟場次層級的臨打/單場季打金額分開）
- 季打名單tab的「繳費狀態」不再是獨立切換的選項，改成完全跟著「繳費方式」自動判斷：選了方式就是已繳，選「未指定」就是未繳
- 原本已繳費的季打若改回未繳費，「本季出席詳情」跟「統計結算」裡顯示的預收金額都會正確變回 $0，不會再顯示舊的預繳數字
- 季打沒有預繳的情況下，不會再出現在「統計結算」頁面（因為沒有預繳就沒有可以拿來計算退費/補收的基準）
- 場次內頁上方的「季打已收」欄位重新定義：只計算「已完成季度預繳」跟「沒有預繳但單場次有繳費」這兩種情況的金額；單純出席但完全沒繳費的季打不會計入這個欄位。這個修正套用到所有會用到這個計算的地方（總覽、場次列表、場次內頁、季度內頁），確保各處數字一致

## v41 更新內容
- 修正「季打未繳費，在場次人員名單設定單場繳費方式後，下方統計仍顯示 $0」的問題。真正原因是「加入季打名單」這個流程，幫該成員在整季每一場建立的名單列，費用金額都寫死是 0（沒有跟著各場次自己設定的季打收費走），導致之後只選繳費方式、沒特地手動改金額時，那個 0 就一直留著。現在加入季打時，每一場的費用會直接帶入那一場自己設定的季打收費金額，同一個地方也順手修正了另外兩處會建立季打列的流程（場次內頁的季打管理頁籤、季度內頁的出席紀錄視窗）

## v40 更新內容
- 修正「未繳費季打選了繳費方式後，會變成已預繳標籤、無法再調回未繳費」的問題。現在只要季度層級是未繳，這一列就會**一直維持下拉選單的樣式**（跟臨打完全一致），選了方式就是已繳、選回「－」就是未繳，可以自由切換，不會再卡死在標籤狀態
- 季打人員的本場次費用，現在建立時會自動帶入這個場次設定的「季打預設收費」金額，不用每次手動輸入；點擊姓名編輯費用時，如果目前金額還是 0（尚未特別設定過），也會改成預先帶入這個預設金額

## v39 更新內容
- 合併你上傳的文字調整（人員名單「其他」分組標籤）
- 場次內頁的人員名單：未繳費的季打人員現在比照臨打，姓名可點擊設定本場次費用、有繳費方式下拉選單。選了任一繳費方式即代表本場次已繳費；已繳費的季打（季度已繳或單場已標記）維持原本的「已預繳」徽章顯示不變
- 季打若採單場次繳費（而非依賴季度預收），這筆金額現在會正確計入場次「總計」卡片的依付款方式加總，跟臨打金額合併顯示；原本「臨打已收/應收」的數字維持不變，不會混入季打金額
- 「加入臨打」「加入候補」「季度管理的加入季打」這三個人員選擇彈窗統一改用同一套元件：都可以搜尋、都支援多選，畫面改為兩欄式排版，checkbox 與姓名一律靠左對齊，欄位寬度固定不會因姓名長短跑版

## v38 更新內容
- 合併你上傳的文字調整（批量操作按鈕文字簡化）
- 場次內頁移除「冷氣與支出」區塊
- 場次內頁「加入臨打」「加入候補」改為可複選（勾選多位人員後一次加入，不用一個一個加）
- 季度詳情「加入季打名單」新增搜尋框，可搜尋人員姓名，原本的多選功能維持不變（切換搜尋內容不會清空已勾選的人）
- **修正一個資料可見性問題**：人員名單頁面原本只依「男」「女」分兩組顯示，如果有人員的性別欄位是空值/未設定（例如透過某些操作意外產生），會完全不出現在畫面上任何地方，連搜尋都找不到，變成「看不見也刪不掉」的殘留資料。現在加了一個「其他（未設定性別）」分組，這類資料一定會顯示出來，可以直接編輯補上性別或刪除

## v37 更新內容
- 修正快速設定送出後自動同步，同步下來一個以上的季度時，header 右上角的季度切換下拉選單沒有跟著更新的問題。原因是這個自動同步流程漏掉了刷新選單的那一步（header 的同步圖示、設定頁的「立即同步」按鈕都有做這一步，只有快速設定的自動同步漏掉），現在三處都一致會刷新選單了

## v36 更新內容
- **修正快速設定貼上失敗的真正原因**：原本的解析邏輯只認得「一行一組」的格式，如果三個值用 `&` 串成同一行貼上，會把第一個冒號後面「一路到底」的內容全部塞進第一個欄位，導致同步密碼跟 LINE 通關密語被吃掉、完全沒有帶入。現在改成同時支援換行分隔跟 `&` 分隔（混用也可以），兩種貼法都能正確解析出三個欄位
- 「自動帶入雲端同步後自動啟用」的行為原本就有做（v35），這次一併確認在 `&` 格式下也正常運作：只要 Worker 網址跟同步密碼都有解析到，就會自動開啟雲端同步並立刻同步一次

## v35 更新內容
- 設定頁「快速設定」：輸入框 placeholder 改為「請輸入」，按鈕文字簡化為「複製」「送出」
- 「送出」現在會直接自動儲存三個欄位（不用再各自按「儲存設定」）；如果 Worker 網址跟同步密碼都有帶到，會自動開啟雲端同步並立刻同步一次
- 季度管理新增時，原本自動建立「每個週日」場次的邏輯改為可選：新增一個「自動產生場次的星期」選單，可選星期日～星期六，預設為「不指定」；選了哪一天，建立季度時就會依起訖日期自動產生每週該天的場次；維持「不指定」則完全不會自動建立任何場次，需要自己到季度內手動新增

## v34 更新內容
- 合併你上傳的樣式調整（設定頁鎖定畫面、季度/場次空狀態的圖示、文字微調）
- 設定頁新增「快速設定」卡片：把 Worker 網址、同步密碼、LINE 通關密語整理成一段文字，可以一鍵複製（會自動存到剪貼簿），也可以貼上後自動帶入對應欄位，不用一個個手動打字。格式很單純（`worker_url:`／`sync_key:`／`line_key:` 各一行，順序不拘、大小寫不拘），貼上後仍需按下方對應的「儲存設定」才會真的存檔
- **修正一個潛在的競態條件**：設定頁如果連續快速按下兩個不同的「儲存」按鈕（例如貼上快速設定後依序按「儲存 LINE 設定」跟「儲存同步設定」），原本可能因為兩個儲存動作幾乎同時進行，其中一個會把另一個還沒寫完的變更蓋掉。現在所有的設定儲存動作都會排隊依序執行，不會再互相覆蓋

## v33 更新內容
- 總覽頁拿掉「補退費試算」區塊

## v32 更新內容
- 合併你上傳的調整版本：編輯圖示放大為 22px、幾處文字標籤簡化（「人員總表」→「人員名單」、「依繳費方式加總」→「總計」、「即將到來的場次」→「最近場次」、「季打應退款／應補收提醒」→「補退費試算」等）
- 你上傳的版本是基於 v30，還沒有 v31 的三項修正（金額對齊頂部、補繳金額拿掉底色外框），這次一併補上，兩邊的調整都保留了

## v31 更新內容
- 總覽頁「季打應退款／應補收提醒」的金額欄位改為對齊頂部
- 季度詳情「統計結算」表格的金額／狀態欄位也改為對齊頂部
- 季打結算需要補繳的金額顯示，拿掉底色與外框，只保留紅色文字，文字大小跟退費金額（綠色）保持一致

## v30 更新內容
- 季打名單、場次人員名單表格的 checkbox 欄位加寬（26px → 44px），跟後面的姓名欄位拉開更多距離

## v29 更新內容
- 修正設定頁「雲端同步」卡片裡的一個標記錯誤（少了一個包住欄位的容器），導致「同步密碼」標題跟輸入框沒有正確換行；現在標題與輸入框各自一行顯示
- 所有「編輯」按鈕（人員總表、季度列表）改用自訂上傳的鉛筆圖示，顏色跟著按鈕本身的顏色走（含滑鼠移過去變色的效果），大小調整為 15px

## v28 更新內容
- 所有 checkbox 都加大跟後面文字/欄位的距離（8px → 14px），降低手機上誤觸的機率；表格內單獨一格的 checkbox 也加了間距
- 季度詳情頁標題區塊改為兩行：場次數量併入標題同一行（例：「2026-Q3・共12場」），不再獨立佔一行
- 場次內頁候補名單的「加入名單」與「移除」按鈕改成強制同一行顯示，加入名單在前、移除在後，不會再上下分開
- **修正常用聊天室同步不到其他裝置的真正問題**：原本 Worker 網址／常用聊天室／繳費方式這三項共用同一個時間戳記，只要任何一項有變動，推送時就會把這三項「當下的完整內容」一起送出去——如果另一台裝置剛好在更晚的時間點只改了其中一項並推送，就會連帶把還沒同步過去的其他兩項一起覆蓋掉，變成「明明已經新增了，卻在別的裝置同步後又不見了」。現在改成三個欄位各自獨立追蹤異動時間、各自同步，彼此不會互相覆蓋
- 修正儲存 Worker 網址時，若結尾多了斜線會導致同步請求誤判成 LINE 轉發請求的問題（自動去除結尾斜線）
- 同步完成後現在會自動刷新目前頁面與 header 季度選單

## v27 更新內容
- 修正「填好同步密碼卻跳出 X-Api-Key 錯誤」的問題：根本原因是儲存的 Worker 網址如果結尾多一個斜線（例如貼上時多了 `/`），組出來的同步網址會變成雙斜線，導致 Worker 判斷成另一種請求、報錯成 LINE 那邊的驗證訊息。現在儲存網址時會自動去掉結尾的斜線，避免這個問題。
- 同步完成（拉取資料）後，現在會**自動刷新目前正在看的頁面**與 header 的季度選單，不用再手動切換頁面或重新整理才看得到剛同步下來的資料
- 合併使用者提供的樣式調整（字型、底部導覽圖示、間距等）

## v26 更新內容
- 雲端同步範圍新增三項非機密設定：Worker 網址、常用聊天室清單、繳費方式清單，會跟著同步引擎自動在裝置間同步，不用每台裝置各自輸入
- 其餘設定（App/設定頁密碼、同步密碼本身、LINE 通關密語、目前選擇的季度）維持每台裝置各自獨立，不會同步——尤其是 LINE 通關密語，特意排除在同步範圍外，避免同步密碼萬一外流時連帶波及 LINE 機器人的發送能力
- 已經在用舊版、本機已經填過 Worker 網址/常用聊天室/繳費方式的人不用重新輸入，下次按同步時會自動把既有設定推上雲端

## v25 更新內容
- 修正「填好同步密碼、勾選啟用、按儲存設定，卻跳出『尚未啟用雲端同步』」的問題。根本原因是雲端同步需要三個條件同時成立（啟用開關、同步密碼、Worker 網址），但「雲端同步」卡片的儲存按鈕只會存前兩個，Worker 網址是另一張「LINE 發送設定」卡片的欄位，要分開存——如果只存了同步那張卡片，Worker 網址就可能是空的，導致同步永遠無法啟用。現在儲存雲端同步設定時，會一併把 Worker 網址欄位目前的內容存進去，不用分兩次存。
- 同步失敗的錯誤訊息也改成明確指出「啟用開關」「同步密碼」「Worker 網址」三者中具體是哪一個沒填，不再是一句籠統的「尚未啟用」

## v24 更新內容
- 專案名稱從「週日場記」改為「隨手場記」（標題、App 名稱、備份檔名等使用者看得到的地方都已更新；內部技術識別碼如資料庫名稱不變，確保原本的本機資料不會因改名而消失）
- 雲端同步改為完全手動觸發：拿掉開機自動同步、恢復連線自動同步、每 60 秒定時同步這些背景機制
- 右上角 header 新增一個同步圖示按鈕，點擊才會執行同步（同步中圖示會旋轉，完成或失敗都會有提示）；設定頁原本的「立即同步」按鈕也還在，兩個都是手動觸發，效果一樣

## v23 更新內容（雲端同步 Phase 2）
- 新增雲端同步引擎：資料仍以本機 IndexedDB 為主（離線一樣正常讀寫），有網路時會自動把本機異動推上雲端、把其他裝置的異動拉下來合併
- 衝突處理採「後寫入者贏」：同一筆資料在兩台裝置都離線編輯過，以最後同步上去的那次為準
- 刪除改為「軟刪除」（保留刪除紀錄，不會真的清空），這樣其他裝置才能透過同步知道「這筆被刪除了」；對一般使用完全無感，畫面上該消失的資料一樣會消失
- 設定頁新增「雲端同步」區塊：填入同步密碼（跟 LINE 設定共用同一個 Worker 網址）、開關同步、「立即同步」按鈕、顯示上次同步時間
- 自動同步時機：App 開啟時、恢復網路連線時、每 60 秒背景嘗試一次；失敗時不打擾使用者（只有手動按「立即同步」才會跳錯誤訊息）
- 需要搭配另外提供的 Worker 更新（`週日場記LINE中介Worker_v2雲端同步.zip`）才能使用，請先完成該 Worker 的 D1 資料庫與 SYNC_KEY 設定

## v22 更新內容
- 「發送到LINE」季打結算訊息的雙欄分法改為依性別分（女生一欄、男生一欄），不再是人數對半均分

## v21 更新內容
- 「發送到LINE」的季打結算訊息改用自訂的雙欄卡片設計：藍色標題「成員退費金額」、成員依人數對半分成兩欄、每列交錯底色、姓名為可點擊的底線連結（帶 postback action）。退費顯示為純數字金額；若該成員還需要補繳，金額前面會加上「+」以區別。

## v20 更新內容
- 修正結算明細彈窗「出席詳情」「請假詳情」按鈕與展開內容之間仍有空隙的問題。真正原因是外層 `.stack` 容器的 flex `gap` 會無視子元素自己的 margin 設定，強制在每個子元素間插入間距；現在把按鈕與展開面板包在同一個小容器裡，讓它們不再被 `.stack` 的 gap 影響，真正接成一個區塊。

## v19 更新內容
- 結算明細彈窗「出席詳情」「請假詳情」的按鈕與展開內容之間拿掉間隙，樣式改成無縫接在一起的一個區塊（類似手風琴效果）
- 出席詳情內容改為顯示：場次日期＋該場次季打預收金額（$）＋該場次人數（括號標示）
- 場地費／冷氣費拆分計算調整：冷氣費 = 冷氣費÷人數，若計算結果個位數不是 0 或 5，會自動無條件進位到最接近的 5 的倍數（例如 44→45、48→50）

## v18 更新內容
- 結算明細彈窗的「出席詳情」「請假詳情」展開內容加上一致的底色卡片樣式，看起來更像一個獨立區塊

## v17 更新內容
- 季度「場次預設值」與場次設定都新增「人數」欄位（預設18），僅用於結算明細拆分場地費/冷氣費占比；在場次管理調整只影響該場次，不影響其他場次
- 結算明細彈窗：預收金額下方新增「場地費」「冷氣費」拆分（依該季打有出席的場次加總），並加上可收合的「出席詳情」清單（預設收合）
- 季打名單點擊姓名的出席紀錄彈窗，移除底部「關閉」按鈕，只保留右上角 ✕
- 冷氣使用分頁的場次列表可點擊，直接跳轉到該場次內頁
- 「依繳費方式加總」統計區塊從統計結算分頁移到季打名單分頁

## v16 更新內容
- 分頁名稱「統計與退款結算」改為「統計結算」
- 結算明細彈窗：請假詳情移到「請假場次」欄位下方，按鈕文字簡化為「請假詳情」，展開後的日期改為兩欄顯示以節省空間

## v15 更新內容
- 拿掉固定標題區塊的底部邊框線
- 統計與退款結算表格重新分配「結果」「狀態」欄寬，並讓按鈕/金額文字不換行，360px 下更穩定
- 季度詳情新增「冷氣使用」分頁（在季打名單後面），列出本季每個場次的冷氣使用狀態
- 結算明細彈窗：請假場次數不為 0 時，新增可展開/收合的請假場次清單（預設收合）
- 結算明細彈窗移除右下角「關閉」按鈕，只保留右上角的 ✕

## v14 更新內容
- 場次內頁標題文字再縮小；修正下滑時固定標題區塊隱約透出下方白色內容/框線的問題（背景延伸滿版並加底部邊框）
- 季度詳情頁標題套用同樣的固定樣式，字級縮小，「季度設定」改用齒輪圖示按鈕
- 季打名單表格移除「金額」「方式」欄位，改成點擊「已繳/未繳」開啟原本的編輯彈窗（內含金額與方式），欄寬重新調整
- 季打出席紀錄彈窗的下拉選單縮小固定寬度，日期與星期不再換行
- 統計與退款結算表格移除「預收」「應付總額」欄位；新增點擊姓名的結算明細彈窗（預收金額、實際應付金額、請假場次、結果、狀態）
- 統計與退款結算分頁新增「發送到LINE」按鈕，可將本季所有季打的結算結果整理成 Flex Message 發送
- 新邏輯：季打若尚未繳費，會在場次人員名單顯示「未繳費」標記，可在該場次單獨切換為已預繳／未繳費，不影響季度層級的繳費狀態或其他場次
- 人員總表（設定內的人員管理）搜尋改為點擊圖示才展開輸入框；頁面加上返回按鈕與固定標題（因為現在是從設定頁進入的內頁）
- 底部導覽「設定」圖示改用自訂圖片

## v13 更新內容
- 場次內頁標題列改版：日期字級縮小、標題區改為兩行（日期／時段），「發送到LINE」與「場次設定」改用圖示按鈕（自訂上傳的圖示），360px 寬度下不再擠壓或換行

## v12 更新內容
- 場次人員名單拿掉「費用」欄位，改成點擊姓名跳出彈窗編輯費用；批量調整費用功能不變
- 人員名單表格加寬姓名欄位，並在 360px 窄螢幕加上安全網（表格內部可橫向捲動），修正刪除按鈕跑出表格外、男女表格超出卡片寬度的問題
- 場次內頁標題日期改為緊湊格式（例：2026/08/02(日)），強制不換行，空間不足時字級會自動縮小
- 設定頁「常用聊天室」的新增欄位改成上下排列，修正輸入框在窄螢幕會突出畫面的問題
- 底部導覽移除「人員」分頁，改成在「設定」頁面新增「人員管理」按鈕進入人員總表
- 設定頁加上密碼保護，需輸入 `setting` 才能進入（同一次使用期間內只需輸入一次）
- App 首次使用需要輸入密碼 `admin` 才能進入，之後這台裝置會記住不用再輸入（僅為基本防護，非真正的資安機制，設定頁裡的邏輯一樣是寫在前端程式碼中）

## v11 更新內容
- 發送到LINE的 Flex Message 卡片改用自訂設計：藍色標題（日期＋時段場地）、女／男兩欄編號名單卡片、候補區塊（每列3人）、底部 Total 統計列（X 女 X 男 (候補 X 人)）。金額資訊已從LINE卡片中移除（原本的「已收/應收」不再顯示）。

## v10 更新內容
- 修正發送到LINE會回傳「A message is invalid / unknown field: opacity」錯誤的問題。原因是 Flex Message 卡片標題裡有一個文字元件誤用了 LINE 不支援的 `opacity` 屬性，已移除並改用實色達到相同的淡化視覺效果。

## v9 更新內容
- 新增「發送到LINE」功能：場次詳情頁新增按鈕，可以把該場次的人員名單（季打出席、臨打含繳費狀況、候補）整理成 LINE Flex Message 卡片，透過你自己架設的 Cloudflare Worker 中介，推播到指定的 LINE 聊天室
- 設定頁新增「LINE 發送設定」區塊：填入 Worker 網址與通關密語，並可以建立多個「常用聊天室」（名稱＋Group ID），發送時從清單挑選要送到哪一個
- 需要搭配另外提供的 `週日場記LINE中介Worker` 專案（部署在你自己的 Cloudflare 帳號下）才能使用，PWA 本身不會、也不能直接持有 LINE 機器人的 Channel Access Token

## v8 更新內容
- 修正 iOS「加入主畫面」的 PWA 模式下，匯出備份顯示「下載完成」但在檔案 App 裡完全找不到檔案的問題。原因是 iOS 待機模式（standalone）的 PWA 沒有 Safari 真正的下載管理機制在背後運作，`<a download>` 這種下載方式會悄悄失效。現在改成優先呼叫系統原生的「分享」面板（Web Share API），可以直接選「儲存至檔案」，這個管道在 iOS PWA 底下是可靠的；只有在裝置/瀏覽器不支援分享檔案時，才會退回原本的下載連結方式。

## v7 更新內容
- 退款金額的負號改用一般減號「-」
- 批量輸入人員的欄位標題改為「手動輸入」，並拿掉輸入框的提示文字
- 「中途加入」標示彈窗內容簡化，只保留加入日期
- 未繳費提醒改用文字變色（紅色），不再用底色，避免跟女生區塊的底色搞混
- 整體文字放大（根字級調整為18px），在 iPhone 11 等手機上更好閱讀，版面不受影響
- 新增／編輯季度時，加入跟場次設定一樣的「場次預設值」（時段、場地、冷氣、費用等）：建立季度時用來產生所有場次，之後調整這些數值會自動套用到本季所有場次；個別場次仍可再單獨調整，只影響那一場
- 有返回鍵的內頁（季度詳情、場次詳情），標題區塊與設定按鈕在往下滑動時會固定在頂部，不會跟著內容捲動

## v6 更新內容
- 修正 header 右上角季度下拉選單：新增/編輯/刪除季度後會立即刷新選單內容（原本要重新整理頁面才會更新）
- 所有可以新增人員的地方（人員總表、加入季打、加入臨打／候補）都支援批量新增：姓名欄位用空白分隔多筆，套用同一個性別，一次建立多筆
- 修正季打「加入日期」邏輯的重大 bug：加入日期之前的場次，現在會明確標記為「請假」；之前版本只是不處理這些場次，導致結算時被誤判為「出席」而多算費用
- 季打名單中，加入日期與季度開始日不同的人員會顯示「中途加入」標示，點擊可看到實際加入日期
- 修正未繳費底色與女生區塊底色相同、難以區分的問題（未繳費改用紅色系底色，與女生區塊的金色底色明確區分）

## v5 更新內容
- 總覽與季度詳情的副標題日期改為只顯示日期（YYYY/MM/DD），不顯示星期；季度詳情的總場次數改為換行顯示
- 季度詳情的三個分頁順序調整為：場次管理、季打名單、統計與退款結算（預設開啟場次管理）
- 「加入季打」新增「加入日期」欄位（預設為季度開始日），只會自動加入該日期之後的場次，之前的場次不受影響
- 季打名單分頁標題加入「男 X 位・女 X 位」統計，並將名單拆成男/女兩個區塊分別顯示，加上顏色區分
- 場次管理列表的副標題改為「季打X人 臨打X人 有/無冷氣」與「已收/應收金額」分兩行顯示
- 場次統計不再計入候補（候補）的金額——候補只是名單上的暫存區，不算業績
- 場次人員名單的說明文字改為「季打X人・臨打X人・候補X人」與「總共X人」分兩行顯示
- 候補區域重新設計：拿掉繳費方式欄位與未繳費底色提醒，改成「加入名單」按鈕，點擊後會依性別自動歸入上方對應區塊並從候補移除
- 人員名單與季打名單的男/女區塊都加上顏色區分樣式

## v4 更新內容
- 修正季度「進行中／已結束」判定邏輯：只看季度的結束日期是否早於今天，尚未開始的季度也算「進行中」
- 季度管理與場次管理的「已結束」清單改為灰底顯示
- 季度管理的日期顯示拿掉星期，只留日期；日期範圍與場次/季打人數改為換行顯示
- 場次的「切換季度」功能整合到右上角 header 的下拉選單，並修正原本切換無效的 bug；場次頁移除原本重複的本地切換季度區塊
- 場次設定新增「季打預設收費」欄位（預設 230），季打人員該場次應付金額改用這個固定金額計算，不再用場地費/冷氣費換算分攤
- 場次「加入臨打」的搜尋名單會排除本季季打成員，並加入姓名搜尋功能
- 場次人員名單中未繳費的臨打/候補會有底色提醒
- 場次人員名單新增「候補」區域（男女下方，不分性別分組但姓名旁會標註性別），可用「加入候補」按鈕新增，資料欄位與臨打相同
- 全部的刪除按鈕統一改成與人員名單一致的「✕」樣式，並讓編輯／刪除按鈕大小一致
- 底部導覽可直接到達的頁面（總覽、季度、場次、人員、設定）拿掉左上角返回箭頭；只有點進去的內頁（季度詳情、場次詳情）才有返回箭頭

## v3 更新內容
- 修正重大時區 bug：自動產生場次的日期原本會在 UTC+8 時區被往前推一天（週日誤判為週六），已修正為完全使用本地時間運算
- 季度管理拆成「進行中」／「已結束」兩區塊，各自依規則排序
- 場次同樣拆成「進行中」／「已結束」兩區塊
- 季度內新增「場次管理」分頁，可直接查看/新增該季場次
- 頂層「場次」分頁右上角可切換季度查看不同季的場次
- 新增場次時可選擇要建立在哪個（進行中的）季度
- 修正新增場次表單輸入框寬度不一致的問題
- 季打管理邏輯改為「預設出席、僅需標記請假」（不再有「未加入」狀態），且依男/女分組顯示
- 加入季打後，該季所有「尚未開打」的場次會自動把新成員加進去（預設出席），已發生的場次不會被回溯更動
- 加入季打名單改為可複選，一次加入多人
- 季打分攤人數改成「場次」維度的設定（每場都可各自調整），不再綁在季度上
- 人員名單中姓名與「季打／臨打」標籤改為換行顯示
- 場次統計新增「季打已收金額」（＝該場次季打出席人數 × 該場次季打應繳金額），並計入盈餘
- 季打結算頁的退款／補收金額改為：退費顯示「－」、需補繳顯示紅色「＋」，金額相符則不顯示正負號
- 季打名單點擊姓名的彈窗，加入「總請假次數」統計，並支援勾選多場次批量調整出席／請假（會即時同步回各場次）
- 所有日期顯示格式統一為 YYYY/MM/DD

## v2 更新內容
- 底部導覽新增「場次」分頁，可直接瀏覽/管理場次，不必先進季度
- 新增季打人員時，預收金額直接帶入季度設定的整季預收金額
- 建立季度後，自動依起訖日期產生每個禮拜日的場次（套用預設時段/場地/冷氣/費用）
- 場次新增「季打管理」分頁：設定某位季打「出席」會自動出現在人員名單，設定「請假」則自動從人員名單移除
- 場次人員名單拿掉「出席」「已繳」欄位，繳費方式為「－」代表未繳，其他即代表已繳
- 「散客」全面改稱「臨打」，加入時費用自動帶入該場次的臨打預設收費
- 場次新增「依付款方式加總」
- 移除「季打人均分攤」顯示；分攤公式改為 (場地費＋冷氣費) ÷ 場地人均分攤人數（季度設定中可調整，預設 18）
- 季打名單點擊姓名可查看/調整整季每場次的出席與請假紀錄
- 季度名稱預設格式改為 `2026-Q3`
- 場次預設值：時段 15:30-18:30、場地「球魔方2.0」、冷氣使用、場地費 3740、冷氣費 810、臨打預設收費 265
- 修正手機上點擊輸入框/下拉選單畫面被放大的問題（所有表單控制項字級調整為 16px）
- 每個頁面左上角新增「←」返回按鈕

## 這是什麼

依照規格書實作的完整功能：
- 季度（3 個月一季）與場次管理，建立季度自動產生全季場次
- 場次人員名單（男/女分組），季打管理獨立分頁控制出席/請假（預設出席）
- 季打名單設定，加入時自動帶入季度預收金額，並可批次多選加入
- 冷氣使用紀錄與支出，分攤人數可依場次個別調整
- 繳費方式紀錄（－ 代表未繳）與批量調整（季打與臨打皆可）
- 費用管理與批量調整（臨打）
- 場次／季度統計：應收、已收、支出、應收盈餘、已收盈餘（皆含季打已收金額）、依繳費方式加總
- 季打退款／應補收自動結算，退費顯示「－」、補收顯示紅色「＋」，並可點擊姓名查看/批量調整每場出席紀錄

## 如何使用（本機開啟）

因為瀏覽器安全限制，Service Worker（離線快取）與 IndexedDB 在 `file://` 直接開啟時可能無法正常運作，**建議用一個簡單的本機伺服器來開啟**：

### 方法一：使用 Python（大多數電腦都有）
```bash
cd 這個資料夾
python3 -m http.server 8080
```
然後用瀏覽器打開 `http://localhost:8080`

### 方法二：使用 Node.js
```bash
cd 這個資料夾
npx serve .
```

打開後，手機瀏覽器可以用「加入主畫面」／桌面瀏覽器網址列的「安裝」圖示，把它安裝成一個 App。

## 如何部署到網路上（讓手機直接連網址使用）

這是純前端靜態網站，沒有後端，可以直接放到任何靜態網站託管服務，例如：
- **GitHub Pages**：把這個資料夾內容 push 到一個 repo，開啟 GitHub Pages 指向該分支即可。
- **Cloudflare Pages / Netlify / Vercel**：把資料夾拖拉上傳即可部署，幾分鐘內拿到一個網址。

部署後用手機瀏覽器打開該網址，一樣可以「加入主畫面」安裝成 App。

## 資料備份

所有資料只存在單一瀏覽器、單一裝置裡。**強烈建議定期到「設定」頁面按「匯出 JSON 備份」**，把備份檔存到雲端硬碟等地方。若要更換裝置或清除瀏覽器資料前，也請先匯出備份，之後可以用「匯入 JSON 備份」還原。

## 檔案結構
```
index.html              主頁面
manifest.webmanifest    PWA 設定檔
sw.js                   Service Worker（離線快取）
css/styles.css          樣式
js/
  main.js               入口、路由註冊
  router.js             簡易 hash router
  db.js                 IndexedDB 封裝
  calc.js               核心計算邏輯（統計、退款試算）
  utils.js              共用工具函式
  views/                 各頁面（首頁、季度、場次、人員、設定）
icons/                  App 圖示
```

## 已知待確認事項（規格書中已定案，若要之後調整可回頭修改 calc.js）
- 季打「中途加入」試算：固定用「季初預估值」計算，不使用實際場次均價。
- 季打退款：以「(場地費＋冷氣費) ÷ 季打人數快照」的場次人均分攤成本計算，請假場次份額視為 0（全額退還）。
